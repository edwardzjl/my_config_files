
# jenv
eval "$(jenv init -)"

# the fuck
eval $(thefuck --alias)

# This should be the last line of the file
# For local changes
# Don't make edits below this
[ -f ".evals.local" ] && source ".evals.local"

